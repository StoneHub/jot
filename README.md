# Porch Speech

Private native macOS speech service: hold Fn to dictate, continuous local ambient transcription, four speaker slots with manual labels, and app/CLI/MCP resource stats.

Implementation in progress. Audio is temporary; transcripts stay local. Ambient speech is context, never an instruction to execute.

See docs/PLAN.md for scope and docs/BACKLOG.md for deferred work.
