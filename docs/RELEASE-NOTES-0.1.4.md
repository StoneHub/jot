# Jot 0.1.4

Jot keeps each ambient row's words with speaker evidence, so Sessions can Regroup a saved session with the current Tuning. Session audio is kept in a private file only until an offline speaker pass finishes, then deleted (Tuning switch to turn it off). Speaker naming from the pass comes next.
