# Cloud batch 2: bounded work with explicit local gates

The first batch produced eight merged PRs: Jot #83/#84/#85, AppleFM VS Code #22/#23, Terminal #11, and Jev #5/#6. ThreadSpace produced a report, now corrected against the private backend. Do not repeat those assignments or assume their original open-status summaries are current. Refresh each selected repository's main, issue and overlapping PR once before work.

## Choose the next outcome

| Repository | Assignment | Cloud evidence | Local gate |
| --- | --- | --- | --- |
| `StoneHub/jev-tab-organizer` | [#3, saved links](https://github.com/StoneHub/jev-tab-organizer/blob/main/docs/cloud-tasks/saved-links.md) | Dependency-free tests, source checks and package; browser smoke only if already available | Real browser save/restart/reopen, two dashboards, extension reload; nothing closes |
| `StoneHub/apple-fm-vscode` | [#20, safe long-block truncation](https://github.com/StoneHub/apple-fm-vscode/issues/20), current `docs/CLOUD-WORK.md` | Pure pipeline tests and over-cap fixtures; no live model or helper packaging | Actual editor/model, parse or abstain, VSIX package/install |
| `StoneHub/jot` | [#86, evaluation executable](https://github.com/StoneHub/jot/issues/86), [packet](suggestion-evaluation-harness.md) | Explicitly uncompiled Swift source PR plus portable checks; no invented model results | Swift build/tests, actual AppleFM output and cold/warm latency review |
| ThreadSpace client `benwilliams0540/t3code` | [current packet](https://github.com/benwilliams0540/t3code/blob/main/docs/rooms/cloud-next-task.md): hosting/recovery design first; Shared cursor recovery is an independent fallback | Source-backed architecture report, or focused decoder/change-loop regression | Architecture decision/spike, or installed Shared reconnect as appropriate |

For ThreadSpace architecture, use the private companion repository only if it is already authorized and accessible, following the client packet's exact path and explicit `main` target. Keep its evidence in that private repository. Do not infer a new backend contract from the public client alone. Missing access is not a reason to recreate the backend.

Jot owns shared context and the eventual Terminal installation/settings experience. Codex is the first use case, not a permanent app boundary. The new Jot assignment measures the foundation; it does not deliver ghost text, universal context capture, a browser adapter or Terminal migration. AppleFM Swift remains pinned and generic until a concrete caller requirement warrants a separate issue.

## Copy-ready dispatch prompt

> Use the selected repository's assignment from StoneHub/jot `docs/cloud-tasks/next-batch.md`. Read its current task packet, issue and repository instructions. Refresh the remote main and check for overlapping work; preserve the runner's provisioned branch. Complete one bounded assignment and return its exact revision, actual checks, unavailable gates and next Mac command. Prefer Jev #3 or VS Code #20 for portable implementation; choose the ThreadSpace hosting/recovery packet for high-level architecture work. Jot #86 is an explicitly uncompiled source-preparation task pending Mac acceptance. Work only in the selected/explicitly available repositories; do not scan the account or try to repair missing private access. Run one five-minute preflight and allow one evidence-backed setup correction. Do not install Apple SDKs on Linux, launch paid providers, widen permissions, change dependencies to evade platform limits or fabricate runtime evidence. Use one worker for the scoped task; no automatic agent teams, hourly checks, watchers, PR polling or auto-merge. Respect the packet's PR-versus-branch workflow. Push the result, report once, and stop. Local Codex owns Mac checks, merge, canonical checkout and installation.

The selected repo determines the task; this is not permission to implement the whole table. Independent assignments can run in separate cloud sessions when desired. Choose the actual model in the launcher: ordinary scoped implementation does not require the most expensive reasoning model; reserve stronger reasoning for the Rooms architecture comparison. Record the selected model and actual usage in the private dispatch record. A prompt dollar limit is not an enforced spending cap.

## What improved after the first batch

- Portable tests and fake-provider boundaries are explicit; Mac SDK/model/packaging failures end at a named handoff.
- The Mac reviewer found output-shaping defects through live model/editor trials after cloud tests passed. Recorded regressions belong in the next portable baseline.
- Jot's local-check tool now rejects a posted report for a different PR revision or a worktree changed during testing. PASS is evidence, not cloud merge authorization.
- Fixture ideals are accurately labeled authored test expectations. Unreviewed semantic scores remain null; the next harness must prove normal prompts cannot see oracle answers.
- Cloud workers stop after their artifact. Waiting for Mac validation is not productive cloud work.

## Delivered versus pending

Jot's export tuning fix is installed from Release runtime revision `19b7f97` with executable SHA-256 `b816c4efa40a0b67782d8cb0426f217f76a1cd1f05cb7cfa829bb5fea5d6f646`. Later commits in this handoff change documentation only. Mac gates passed: 187 Swift tests, 35 Python tests, 12 validated scenarios, signed Debug/Release builds and recovery-controller checks. History counts were preserved across installation. Public notarized distribution was not performed.

VS Code 0.1.9 and Terminal 0.1.1 have local installed/runtime proof. Existing editor windows and shell sessions may still hold the older loaded code until reloaded; preserve their work. See their repository acceptance records rather than interpreting a package/version bump as proof by itself.

Jev's latest Tidy preview is merged and packaged, with 15 tests plus isolated Chromium acceptance. The user chose to load it into Edge later: Manage extensions → Developer mode → Load unpacked → the canonical repository's `extension/` folder. The extension-manager route was blocked by local automation policy, so installed-user-profile proof remains pending. See [Jev acceptance](https://github.com/StoneHub/jev-tab-organizer/blob/main/docs/LOCAL-ACCEPTANCE-2026-09-25.md).

ThreadSpace's changes are documents only. Installed Alpha 0.0.37 was not replaced or claimed as newly verified from current source. Existing feature checkouts and untracked work were preserved. The [reviewed report](https://github.com/benwilliams0540/t3code/blob/main/docs/rooms/cloud-product-review.md) and separate private packet carry the next decision; no hosting migration or public deployment occurred.
