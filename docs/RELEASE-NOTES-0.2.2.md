# Jot 0.2.2

Only one Jot on disk: installs and updates no longer leave old copies that Spotlight can open, and a Release build opened from anywhere but Applications redirects to the installed app. Release script gains --local for development-signed pre-releases.
